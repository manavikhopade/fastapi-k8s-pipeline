[project]
name = "fitcheck"
version = "0.1.0"
description = "Daily fitness check-in API"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.110",
    "uvicorn[standard]>=0.29",
    "sqlalchemy>=2.0",
    "pydantic>=2.6",
]

[dependency-groups]
dev = [
    "pytest>=8.0",
    "httpx>=0.27",
]
