apiVersion: v2
name: fitcheck
description: Helm chart for the FitCheck daily fitness check-in app
type: application
version: 0.1.0          # the chart's version
appVersion: "0.1.0"     # the app's version